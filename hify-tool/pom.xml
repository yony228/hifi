<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.hify</groupId>
        <artifactId>hify</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>hify-tool</artifactId>
    <packaging>jar</packaging>

    <name>Hify Tool</name>
    <description>
        工具系统 — 工具注册与发现。
        支持四种工具类型：网页搜索（SerpAPI/Bing）、代码执行（Docker 沙箱）、HTTP 自定义工具、MCP 工具。
        管理 tool 和 agent_tool 两张表，提供工具注册表和工具调用执行。
        依赖：hify-common。
    </description>

    <dependencies>
        <!-- 内部模块 -->
        <dependency>
            <groupId>com.hify</groupId>
            <artifactId>hify-common</artifactId>
        </dependency>

        <!-- Spring Boot Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- MyBatis-Plus -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
        </dependency>

        <!-- Spring AI MCP Client（MCP Server 自动发现 + 工具注册）
             注：Spring AI 1.0.0-M6 中 MCP 为实验性功能，artifact 名可能变更。
             升级到 GA 后启用此依赖，当前 MCP 工具通过 HTTP Client 手动实现。 -->
        <!--
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-mcp-client</artifactId>
        </dependency>
        -->
    </dependencies>

</project>
